{
  "compilerOptions": {
    "baseUrl": "src", // This must be specified if "paths" is.
    "paths": {
      "models": ["models"],
      "config": ["config"],
      "user": ["user"]
    },
    "esModuleInterop": true
  }
}
