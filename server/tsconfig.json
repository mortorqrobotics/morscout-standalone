{
  "compilerOptions": {
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "models": ["src/models"],
      "config": ["src/config"],
      "user": ["src/user"],
      "api/*": ["src/api/general/*"],
      "cli": ["src/cli"],
      "setup/*": ["config/jest/setup/*"],
      "teardown/*": ["config/jest/teardown/*"]
    },
    "esModuleInterop": true
  }
}
